[package]
name = "sex"
version = "0.1.0"
edition = "2024"

[dependencies]
sex_derive = { path = "derive", optional = true }
sex_util = { path = "util" }

[features]
default = ["derive"]
derive = ["sex_derive"]

